#ALGORITHM & CALCULUS 1

USING MATH MODULE (PYTHON)

ALGORITHM & CALCULUS 1

FIND THE VALUE OF (X) = FORMULA: f(x)=(ax^2+bx+cx)-simplify

1. Solve a linear equation (ax + b = c)
2. Solve a quadratic equation (ax^2 + bx + c + 0)
3. Evaluate f(x) = ax^2 + bx + c
4. Find the derivate f'(x) at a point
5. Estimate a limit as x approaches a value
6. Compute a definite intergal of f(x)
7. Quit

#ALGORITHM & CALCULUS

import_math:

#----- MAIN PROGRAM -----
def main():
    print("ALGEBRA & CALCULUS 1")
    print("FIND THE VALUE OF (X) = FORMULA: f(x)+(ax^2+bc+c) ")

    while True:
        print("\n1. Solve a linear equation (ax + b = c)")
        print("\n2. Solve a quadratic equation (ax^2 + bx + c + 0)")
        print("\n3. Evaluate f(x) = ax^2 + bx + c")
        print("\n4. Find the derivate f'(x) at a point")
        print("\n5. Estimate a limit as x approaches a value")
        print("\n6. Compute a definite intergal of f(x)")
        print("\n7. Quit")

        choice = input("Choose an option: ").strip()

    if choice == "1":
        a,b,c = get_float("a: "), get_float("b: "), get_float("c: ")
        print(solve_linear(a,b,c))

    elif choice == "2":
        a,b,c = get_float("a: "), get_float("b: "), get_float("c: ")
        print(solve_quadratic(a,b,c))

    elif choice == "3":
        a,b,c = get_float("a: "), get_float("b: "), get_float("c: ")
        x = get_float("x: ")
        print(f"f({x}) = {f(a,b,c)} ")

    elif choice == "4":
        a,b,c = get_float("a: "), get_float("b: "), get_float("c: ")
        exact = derivative_exact(a, b, x)
        numeric = derivative_numeric(a, b, c, x)
        print(f" Numeric f'({x}) = {numeric:.6f} (via central difference)")

    elif choice == "5":
        a,b,c = get_float("a: "), get_float("b: "), get_float("c: ")
        x = get_float("x0(the value of x approaches): ")
        print(estimate_limit(a,b,c,x0))

    elif choice == "6":
        a,b,c = get_float("a: "), get_float("b: "), get_float("c: ")
        x1 = get_float("Lower bound x1: ")
        x2 = get_float("Upper bound x2: ")
        exact = integral_exact(a, b, c, x1, x2)
        numeric = integral_trapezoid(a, b, c, x1, x2)
        print(f"Exact integral = {exact:.6f}" )
        print(f"Numeric integral = {numeric:.6f}" (via trapezoidal rule, n = 1000)" ")

    elif choice == "7":
        print("Goodbye!")
        break

    else:
        print("Invalid choice: Please choose 1-7.")

    if_name_=="_main_":
        main()

import math
#----- ALGEBRA -----
def sollve_linear
def solve_quadrati
#----- CALCULUS 1 -----
def derivative_exact
def derivative_numeric
def estimate_limit
def integral_exact
def integral_trapezoid
#----- INPUT HELPER -----

#----- MAIN PRGRAM -----
def main():
